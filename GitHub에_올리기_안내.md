# 🚀 GitHub에 코드 올리기 (쉽게 따라하기)

> 초등학생도 할 수 있도록 **한 단계씩** 적었어요.  
> 막히면 그 단계만 다시 읽어 보세요!

---

## ⚠️ "git은 내부 또는 외부 명령...이 아닙니다" 가 나왔을 때

**이 메시지 = "Git 프로그램이 아직 컴퓨터에 없어요"** 라는 뜻이에요.  
아래 **"Git 설치하기"** 를 **처음부터 끝까지** 한 번만 따라해 주세요.

---

## 📌 1. Git 설치하기 (가장 중요! 먼저 하기)

> 💡 **한 번만** 설치하면, 앞으로는 `git_commit_push.bat` 만 더블클릭해서 쓸 수 있어요.

### ① 다운로드 페이지 들어가기

1. **크롬**이나 **엣지** 같은 **인터넷 창**을 연다.
2. **주소창**(맨 위에 url 쓰는 칸)을 클릭한다.
3. 아래 주소를 **그대로** 복사해서 붙여 넣고 **엔터** 친다.
   ```
   https://git-scm.com/download/win
   ```

### ② 설치 파일 받기

1. 페이지가 열리면 **"Click here to download"** 라고 된 **파란 버튼**이 보인다.
2. 그 버튼을 **한 번 클릭**한다.
3. **다운로드**가 된다 (보통 왼쪽 아래 또는 바탕화면에 `Git-2.43...-64-bit.exe` 같은 이름으로 받아져요).

### ③ 설치 파일 실행하기   

1. **다운로드된 파일**을 찾아서 **더블클릭**한다.
2. **"사용자가..." 실행해도 되냐고** 물어보면 **"예"** 를 누른다.

### ④ 설치할 때 Next만 누르기

1. **"Next"** → 계속 **Next** 만 눌러도 된다.
2. **중요:** **"Adjusting your PATH environment"** 이라고 나오는 화면이 있으면  
   → **"Git from the command line and also from 3rd-party software"** 가 선택돼 있는지 보고,  
   → 아니면 그걸 **골라 준 다음** **Next** 한다.
3. 나머지도 **Next** 로 넘기다가 **"Install"** 이 나오면 **Install** 을 누른다.
4. 설치가 끝나면 **"Finish"** 를 누른다.

### ⑤ 컴퓨터를 한 번 껐다 켜기

1. **시작 버튼** (윈도우 창문 모양) 누르기
2. **전원** → **다시 시작** 또는 **종료 후 다시 켜기**

> ⭐ **끝나면** 아래 **"2. Git이 잘 깔렸는지 확인하기"** 로 가서 확인해 보세요.

---

## 📌 2. Git이 잘 깔렸는지 확인하기

1. **윈도우 키** (키보드 맨 아래 조그만 창문 모양) 누르기
2. **"cmd"** 라고 입력하기
3. **명령 프롬프트** (검은 창) 가 보이면 더블클릭해서 열기
4. 검은 창에 **`git --version`** 이라고 입력하고 **엔터** 누르기

- **"git version 2.43..."** 처럼 **숫자**가 나오면 → ✅ **성공!** 이제 **3단계**부터 하면 돼요.
- **"git은(는) 내부 또는 외부 명령...이 아닙니다"** 가 또 나오면  
  → 컴퓨터를 **한 번 껐다 켠 다음** 다시 위 **"2. Git이 잘 깔렸는지 확인하기"** 를 해 보세요.  
  → 그래도 안 되면 **"1. Git 설치하기"** 를 **한 번 더** 처음부터 따라해 보세요.

---

## 📂 우리가 할 일: "코드를 GitHub 웹사이트에 올리기"

- **코드** = 지금 만든 "능친 만들기" 프로그램 파일들  
- **GitHub** = 그 코드를 인터넷에 안전하게 보관해 주는 사이트

---

## 🎯 3. 실제로 GitHub에 올리기 (Git 설치한 다음에!)

### 3-① 프로젝트 폴더 열기

1. **파일 탐색기** (폴더 모양 아이콘) 를 연다.
2. 왼쪽에서 **"바탕화면"** 을 누른다.
3. **"정보 cursor"** 폴더를 연다.
4. 그 다음 **"정보 cursor (3)"** 폴더를 연다.  
   → 이게 **우리 프로젝트 폴더**예요.

> 💡 여기 안에 `main.py`, `git_commit_push.bat` 같은 파일들이 보여야 해요.

---

### 3-② 올리기 프로그램 실행하기

1. **"정보 cursor (3)"** 폴더 안에서  
   **`git_commit_push.bat`** 이라는 파일을 찾는다.
2. 그 파일을 **한 번 더블클릭** 한다.
3. **검은 창**이 떠서 영어/한국어가 막 쭉 나온다 → **그냥 두면 돼요.**
4. 맨 아래에 **"아무 키나 누르세요"** 라고 나오면  
   → **엔터** 한 번 누른다.
5. 창이 닫히면 **끝** 이에요.

이걸 한 번 실행하면,  
**코드를 모아서 → GitHub 주소로 자동으로 보내 주는 일**을 배치 파일이 대신 해 줍니다.

---

### 3-③ GitHub에서 확인하기

1. 인터넷 브라우저(크롬, 엣지 등)를 연다.
2. 주소창에 입력: **https://github.com/rlawldks56/making.nj.friend**
3. **엔터** 를 누른다.
4. **로그인** 하라고 나오면, GitHub 쓰는 이메일/비밀번호로 로그인한다.
5. 화면에 **파일 이름들** (main.py, README.md 등) 이 보이면  
   → **제대로 올라간 거예요.**

---

## ❓ 자주 겪는 일 (문제 해결)

### "git은(는) 내부 또는 외부 명령...이 아닙니다" 라고 나올 때

- **Git이 아직 설치 안 됐다는 뜻**이에요.
- 이 글 **맨 위**에 있는 **"1. Git 설치하기"** 를 **처음부터 끝까지** 따라해 주세요.
- 설치 끝낸 뒤 **컴퓨터를 껐다 켜고**, **"2. Git이 잘 깔렸는지 확인하기"** 한 다음에  
  **3-②** (git_commit_push.bat 더블클릭) 를 다시 해 보세요.

### "푸시 실패" / "인증 실패" 라고 나올 때

- GitHub **로그인**이 안 된 상태일 수 있어요.
- 브라우저로 **https://github.com/rlawldks56/making.nj.friend** 들어가서 로그인한 다음,
- 다시 **git_commit_push.bat** 더블클릭을 한 번 더 해 보세요.

### "커밋할 변경사항이 없습니다" 라고 나올 때

- **이미 다 올라간 상태**라는 뜻이에요. ✅ 성공한 거예요.
- 나중에 코드를 수정한 뒤에 다시 **git_commit_push.bat** 를 더블클릭하면, 그때 수정한 것만 또 올라가요.

---

## 📋 한 번에 보는 체크리스트

- [ ] **1. Git 설치하기** 끝냈는지
- [ ] **2. Git이 잘 깔렸는지** 확인했는지 (`git --version` 에 버전이 나오는지)
- [ ] **정보 cursor (3)** 폴더까지 열었는지
- [ ] **git_commit_push.bat** 더블클릭해서 실행했는지
- [ ] 브라우저로 **https://github.com/rlawldks56/making.nj.friend** 들어가서 파일이 보이는지

---

**이 순서만 따라하면, 코드가 GitHub에 올라가요.**  
막히는 단계가 있으면 그 단계 번호만 적어서 다시 물어보면 돼요.

---

## 🌐 "주소 하나로 인터넷에서 쓰고 싶다" (웹 배포)

- 지금 구조는 **로컬 전용**이라,  
  **"https://어떤주소.com"** 형태로 웹에서 접속해서 쓸 수 있는 주소는 **아직 없어요.**
- **나중에 웹으로 만들거나 배포**하면,  
  예를 들어 이런 식이 됩니다:
  - **Streamlit Cloud** 예: `https://making-nj-friend-xxxx.streamlit.app`
  - 또는 다른 호스팅: `https://making-nj-friend.호스팅사이름.com`
- **이건 코드를 웹용으로 바꾸고, 호스팅에 올려야 생기는 주소**예요.
- **자세한 방법**은 같은 폴더에 있는 **`웹_배포_안내.md`** 를 열어 보세요.  
  그 안에 **Streamlit Cloud** 로 웹 주소 만드는 단계가 **쉽게** 적혀 있어요.
