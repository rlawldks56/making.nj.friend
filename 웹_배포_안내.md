# 🌐 "주소 하나로 인터넷에서 쓰기" — 웹 배포 안내

지금은 **로컬(내 컴퓨터)** 에서만 쓰는 구조예요.  
**"https://어떤주소.com"** 형태로 웹에서 접속해서 쓰려면, **웹용으로 올려두는(배포)** 과정이 필요해요.

이 글은 **Streamlit Cloud** 에 올려서  
`https://xxxxx.streamlit.app` 같은 **웹 주소**를 만드는 방법을 단계별로 적은 거예요.

---

## 🎯 하면 좋아지는 것

- **웹 주소 예시**: `https://making-nj-friend-xxxx.streamlit.app`  
  → 인터넷만 되면 누구나 이 주소로 접속해서 "능친 만들기"를 쓸 수 있어요.
- **GitHub Pages** 주소 예: `https://rlawldks56.github.io/making.nj.friend`  
  → 지금 프로젝트는 **Streamlit 웹 앱**이라, **Streamlit Cloud** 로 배포하는 게 맞아요.  
  (GitHub Pages는 정적 페이지용이라 이 앱에는 쓰기 어렵습니다.)

---

## 📌 1. 먼저 GitHub에 코드 올리기

웹 배포는 **GitHub 저장소**를 기반으로 해요.

1. **GitHub에_올리기_안내.md** 대로 따라해서  
   코드가 **https://github.com/rlawldks56/making.nj.friend** 에 올라가 있게 해 주세요.
2. `streamlit_app.py`, `requirements.txt` 가 저장소 안에 포함돼 있어야 해요.

---

## 📌 2. Streamlit Cloud 에서 "웹 주소" 만들기

### ① Streamlit Cloud 들어가기

1. 브라우저에서 **https://share.streamlit.io** 접속
2. **"Sign up with GitHub"** 로 GitHub 계정 로그인

### ② 새 앱 만들기

1. **"New app"** 클릭
2. **Repository** : `rlawldks56/making.nj.friend` 선택
3. **Branch** : `main`
4. **Main file path** : `streamlit_app.py` 입력
5. **App URL** (원하면 바꿀 수 있음):  
   예) `making-nj-friend` 로 두면  
   → 나중에 주소가  
   `https://making-nj-friend-XXXX.streamlit.app`  
   같은 형태로 생깁니다.
6. **"Deploy!"** 누르기

### ③ 배포 후 나오는 주소

- 잠시 지나면 **"Your app is live!"** 라고 나오면서  
  **앱 주소**가 하나 생깁니다.
- 그 주소가 곧 **"주소 하나로 인터넷에서 쓰는 주소"** 예요.  
  예: `https://making-nj-friend-xxxx.streamlit.app`

---

## 📌 3. 실제 데이터(Firebase) 쓰려면 — Secrets 넣기

- **Secrets 안 넣으면**  
  → 웹 앱은 **테스트 모드**로 돌아가요. (데이터는 저장되지 않음)
- **실제 프로필·매칭·채팅**을 쓰려면  
  → Streamlit Cloud 에서 **Firebase 서비스 계정 키**를 Secrets 로 넣어줘야 해요.

### ① Streamlit Cloud 대시보드에서

1. **앱** 클릭 → **"Settings"** → **"Secrets"**
2. 아래처럼 **TOML** 형식으로 적어 넣기  
   (`firebase_config.json` 내용을 한 줄씩 그대로 옮기면 됨):

```toml
[firebase]
type = "service_account"
project_id = "여기에_프로젝트_ID"
private_key_id = "여기에_private_key_id"
private_key = "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
client_email = "여기에_client_email"
client_id = "여기에_client_id"
...
```

- `firebase_config.json` 을 열어서  
  `type`, `project_id`, `private_key_id`, `private_key`, `client_email` 등  
  **이름이 같은 항목**을 그대로 **[firebase]** 아래에 옮기면 됩니다.

3. **Save** 하고, 앱을 한 번 **재실행**(Rerun) 하거나 새로고침해 보세요.

---

## 📌 4. 로컬에서 웹 버전 미리 해보기

배포하기 전에, 내 컴퓨터에서 웹처럼 돌려보고 싶다면:

1. **명령 프롬프트** 또는 **Cursor 터미널**을 연다.
2. 프로젝트 폴더(`정보 cursor (3)`)로 이동한다.
3. 아래 명령을 입력한다.

```bash
streamlit run streamlit_app.py
```

4. 브라우저에 `http://localhost:8501` 이 뜨면  
   그 화면이 나중에 **웹 주소로 배포했을 때**와 같은 흐름이에요.

---

## 📋 정리 — "웹 주소"가 생기는 순서

| 순서 | 할 일 | 결과 |
|------|--------|------|
| 1 | GitHub에 코드 올리기 (git_commit_push.bat 등) | 저장소에 `streamlit_app.py` 있음 |
| 2 | Streamlit Cloud → New app → repo·branch·`streamlit_app.py` 지정 후 Deploy | `https://xxxx.streamlit.app` 주소 생성 |
| 3 | (선택) Secrets에 Firebase 키 넣기 | 실데이터(프로필·매칭·채팅) 사용 가능 |

---

**이 순서만 따라하면,**  
**"https://어떤주소.streamlit.app"** 형태의 **웹 주소**를 쓰면서  
인터넷에서 "능친 만들기"를 쓸 수 있게 됩니다.

궁금한 단계가 있으면 그 단계 번호를 적어서 물어보면 돼요.
